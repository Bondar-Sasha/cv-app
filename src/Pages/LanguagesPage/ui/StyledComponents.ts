import {styled} from '@mui/material'

export const ProficiencySpan = styled('span')(({theme}) => ({
  color: theme.palette.text.primary,
}))

export const LanguageSpan = styled('span')(({theme}) => ({
  color: theme.palette.text.secondary,
}))
